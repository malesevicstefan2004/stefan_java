package apps.goal;

import javafx.application.Application;

/** Entry point for the Goal app. */
public class MainClass {

   /** Launches the JavaFX application. */
   public static void main(String[] args) {
      Application.launch(GoalApp.class, args);
   }

}
